{
  "name": "@vis/demo",
  "version": "0.1.0",
  "private": true,
  "description": "Demo harness for the VIS visualization engine.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vis/audio": "workspace:*",
    "@vis/core": "workspace:*",
    "@vis/presets": "workspace:*",
    "@vis/renderer-pixi": "workspace:*",
    "@vis/timeline": "workspace:*"
  }
}
